Выполняю ваше требование. Ниже представлено только задание 3.3 Сортировка данных.
3.3 Сортировка данных
 * Добавить в приложение возможность указания параметра сортировки (по названию, по длительности, по году выпуска, по убыванию года выпуска).
 * Для указания параметра сортировки использовать переключатели или выпадающий список.
Готовая реализация (с использованием Выпадающего списка Picker)
Это готовый код для реализации сортировки, который использует LINQ-запросы в ViewModel.
1. ⚙️ Логика ViewModel (C#)
В классе MoviesViewModel вам необходимо добавить свойство SortBy и обновить метод, который отвечает за загрузку и обновление списка.
// Фрагмент из ViewModels/MoviesViewModel.cs

// 1. Свойство для привязки выбранного параметра сортировки
private string _sortBy = "Название";

public string SortBy
{
    get => _sortBy;
    set
    {
        if (_sortBy != value)
        {
            _sortBy = value;
            // Вызов метода, который применяет сортировку
            ApplyFiltersAndSort(); 
            OnPropertyChanged();
        }
    }
}

// 2. Логика сортировки внутри основного метода обновления списка
private void ApplyFiltersAndSort()
{
    // Предполагаем, что filteredMovies — это уже отфильтрованный или полный список фильмов.
    IEnumerable<Movie> filteredMovies = ... ; // Ваш текущий список

    // Применение LINQ-сортировки
    IOrderedEnumerable<Movie> sortedMovies;
    switch (SortBy)
    {
        case "Длительность":
            // Сортировка по возрастанию длительности
            sortedMovies = filteredMovies.OrderBy(m => m.Duration);
            break;
        case "Год выпуска":
            // Сортировка по возрастанию года выпуска
            sortedMovies = filteredMovies.OrderBy(m => m.ReleaseYear);
            break;
        case "Убывание года выпуска":
            // Сортировка по убыванию года выпуска (OrderByDescending)
            sortedMovies = filteredMovies.OrderByDescending(m => m.ReleaseYear);
            break;
        case "Название":
        default:
            // Сортировка по названию (по умолчанию)
            sortedMovies = filteredMovies.OrderBy(m => m.Title);
            break;
    }
    
    // ... (дальнейшая пагинация и обновление DisplayedMovies) ...
}

2. 🖥 Интерфейс XAML (MoviesPage.xaml)
Этот код добавляет на страницу элемент Picker (выпадающий список) и привязывает его к свойству SortBy.
<Label Text="Параметр сортировки:" FontSize="Medium"/>

<Picker Title="Выберите тип сортировки"
        SelectedItem="{Binding SortBy}">
    <Picker.ItemsSource>
        <x:Array Type="{x:Type x:String}">
            <x:String>Название</x:String>
            <x:String>Длительность</x:String>
            <x:String>Год выпуска</x:String>
            <x:String>Убывание года выпуска</x:String>
        </x:Array>
    </Picker.ItemsSource>
</Picker>

